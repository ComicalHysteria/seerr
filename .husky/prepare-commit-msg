#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# Run Commitizen only for interactive terminals. GUI commits may not expose /dev/tty.
if [ -t 0 ] && [ -r /dev/tty ]; then
	exec < /dev/tty && npx cz --hook || true
fi
